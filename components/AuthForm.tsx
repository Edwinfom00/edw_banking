import React from "react";

const AuthForm = ({ type }: { type: string }) => {
  return (
    <section className="auth-form">
      <header className="flex flex-col gap-5"></header>
    </section>
  );
};

export default AuthForm;
